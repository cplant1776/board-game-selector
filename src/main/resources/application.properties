
# DataSource settings
spring.datasource.url=jdbc:mysql://localhost:3306/bg_games
spring.datasource.username=bgserver
spring.datasource.password=pw

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# Prevent templates from requiring xml-compliant syntax
spring.thymeleaf.mode = HTML

# Set file size limits
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=15MB

#
spring.http.multipart.max-file-size=15MB
spring.http.multipart.max-request-size=15MB


# =====================================
# SYSTEM DEPENDENT VARIABLES
# =====================================

# For adding a resource directory, ex:
# unix - "file:/opt/files/"
# windows - "file:///C:/opt/files/"
spring.dataset.projectRoot = ${UPLOAD_PATH_RESOURCE}

# Physical locaiton of added resource directory, ex:
# unix - " "/opt/files"
# windows - "C:\opt\files
spring.dataset.uploadPath = ${UPLOAD_PATH_DIR}